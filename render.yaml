services:
  - type: web
    name: django-app
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn backend.wsgi
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: DEBUG
        value: False
